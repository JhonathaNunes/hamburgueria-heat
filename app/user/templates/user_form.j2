{% extends "admin_base.j2" %}
{% if user %}
    {% block aditional_header %}
    <script>
        function deleteUser() {
            if (!confirm("Você tem certeza?")) return;
            xhr = new XMLHttpRequest();
            xhr.open('DELETE', "/admin/user/{{user.id}}");
            xhr.setRequestHeader('Content-Type', 'application/x-www-form-urlencoded');
            xhr.onload = function() {
                if (xhr.readyState !== 4) return;
                if (xhr.status === 200|| xhr.status === 404) {
                    window.location.replace("{{url_for("user.index")}}");
                } else {
                    alert('Erro ' + xhr.status);
                }
            };
            xhr.send();
        }

        function apenasCaracteres(elemt){
            let regex = /[^A-Za-z ?à-ú?À-Ú']/g;
            elemt.value = elemt.value.replaceAll(regex, "");
        }

    </script>
    {% endblock %}
{% endif %}
{% block content %}
<h2 class="page-title">{{title}}</h2>
<div class="d-flex justify-content-center align-items-center">
    <form class="form-style card" method="POST" action="{{action}}">
        <div class="form-group">
            <label for="name">Nome</label>
            <input type="text" id="name" name="name"  onkeyup="apenasCaracteres(this)" onchange="apenasCaracteres(this)"  pattern="[A-Za-z ?à-ú?À-Ú']{3,255}" class="form-control" required value="{{user.name}}"/>
        </div>
        <div class="form-group">
            <label for="description">Nome do Usuário</label>
            <input type="text" id="username" name="username" pattern="[A-Za-z0-9_]{3,255}" class="form-control" required value="{{user.username}}"/>
        </div>
        <div class="form-group">
            <label for="description">Senha</label>
            <input
                type="password"
                id="password"
                maxlength="255"
                name="password"
                class="form-control"
                {% if not user %} required {% endif %}/>
        </div>
        <div class="form-group">
            <label for="description">Permissão do Usuário</label>
            <div class="form-check">
                <input
                    class="form-check-input"
                    type="radio"
                    id="adm"
                    required
                    name="role"
                    pattern="[A]"
                    value="A"
                    {% if user.role.code == 'A' %} checked {% endif %}
                >
                <label class="form-check-label" for="adm">
                    Administrador
                </label>
            </div>
            <div class="form-check">
                <input
                    class="form-check-input"
                    type="radio"
                    id="usr"
                    required
                    name="role"
                    pattern="[U]"
                    value="U"
                    {% if user.role.code == 'U' %} checked {% endif %}
                >
                <label class="form-check-label" for="usr">
                        Usuário Comum
                </label>
            </div>
        </div>
        <div class="form-group d-flex justify-content-between">
            <button type="button" class="btn button-outlined" onClick="history.back()">Cancelar</button>
            {% if user %}
            <button type="button" class="btn button-outlined" onClick="deleteUser()">Excluir</button>
            {% endif %}
            <button type="submit" class="btn button-dark">Salvar</button>
        </div>
    </form>
</div>
{% endblock %}
